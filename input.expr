programa

	declare INTEGER a,b,c,d.
	declare REAL f.
	escreva("Programa Teste").
	escreva ("Digite A").
	leia (a).
	escreva ("Digite B").
	leia (b).	
	a:=1.
	while(a>=1.1){
		if(1>=1){
	 		leia (b).
	 		while(1<10){
	 			leia (c).
	 		}
		}else{
	 	 	leia (b).
		}
	}
	
	do{
		leia (b).
	}while(1<10).
	
	escreva ("C e igual a ").
	escreva (c).
	
	leia (b).
	
	escreva ("D e igual a ").
	escreva (d).

fimprog.